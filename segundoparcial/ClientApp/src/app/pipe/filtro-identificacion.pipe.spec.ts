import { FiltroIdentificacionPipe } from './filtro-identificacion.pipe';

describe('FiltroIdentificacionPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroIdentificacionPipe();
    expect(pipe).toBeTruthy();
  });
});
